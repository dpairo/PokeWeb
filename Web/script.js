document.addEventListener("DOMContentLoaded", function() {
    /*const pokedexSection = document.getElementById('pokedexSection');
    const pokeSearch = document.getElementById('pokeForm');
    const sections = document.getElementById('sections');

    if (pokedexSection && pokeSearch) {
        pokedexSection.addEventListener('click', function() {
            pokeSearch.classList.toggle('collapsed');
            sections.classList.toggle('collapsed');
            console.log(sections.className);
    });*/

    
});